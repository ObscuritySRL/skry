RELENTLESSLY PERFECT umbriel at D:\Projects\umbriel — an AI's substrate to DRIVE NATIVE WINDOWS by EVERY means: UIA tree, a11y/MSAA/JAB, pixels/WGC/OCR, window/process state, events, clipboard, message+pattern acting, DIRECT VALUE-SET (no keystrokes). Cursor-free, offscreen, BACKGROUND by default — FOREGROUND on demand. Pure TS, Bun FFI, NO build. Win32 ONLY via @bun-win32/* (D:\Projects\bun-win32) — never hand-roll FFI. YOU ARE THE END USER. Fable 5 + ultracode all session.

COMPLETE OS SUBSTRATE: do ANYTHING on this machine through umbriel's OWN tools — NEVER need PowerShell/cmd/a CLI. Kill/suspend/inspect processes; read memory/CPU/disk/uptime/power/sleep/standby/lock/display; move/copy/delete/mkdir files (os/fs gate); registry/services/env/network. A task that makes the AI reach for a shell is a GAP to close natively. BENCHMARK, don't assume: choose @bun-win32 FFI vs Bun-native per MEASURED perf (if Bun's FS beats an FFI round-trip, use Bun) — the rule is no-shell, not bun-win32-always.

BAR = HUMAN-TRANSCENDENCE: act on LOCKED/OCCLUDED/OFFSCREEN/VIRTUALIZED/INVISIBLE/never-painted controls; SET values directly (ValuePattern/RangeValue/Toggle/SelectionItem, WM_SETTEXT, LegacyIAccessible) over typing. PARITY LAW: background DEFAULT, foreground FIRST-CLASS — all both ways. No path? INVENT one (BG + FG), PROVE it LIVE, or document the OS wall (UIPI/secure-desktop/foreground-lock).

TWO PANELS. FIRST ACTION EVERY TURN: spawn BOTH, one ultracode agent PER seat, a NAMED EXPERT.
A — CRITIC (audit what EXISTS; presume fault): Perf·Agent-Ergonomics·BG+FG-Parity·Segfault-Safety·Security/Policy·Scenario-Coverage·Doc-Fidelity·Fabrication/Adversarial-Verify·Competitive-Parity·Code-Hygiene.
B — CAPABILITY (hunt what's MISSING; INVENT beyond current limits): close every gap a dev/user could ask — direct value-set, invisible/offscreen acting, any UI framework, grids/trees/docs, files/registry/processes/services, the FULL OS-control surface, multi-monitor/RDP/headless, waits, events, drag-drop, record-replay, self-healing selectors. New tools, each BG+FG + policy-gated.

Every seat RESEARCHES HANDS-ON, NEVER from memory: PROBE live diverse apps (Win32/WinUI/UWP/WPF/Electron/Java/Qt/Office/terminals/UAC/games/RDP) for what fails/lags/needs focus/is unsafe; READ primary sources (MS Learn, headers, @bun-win32/* bindings); STUDY + EXCEED rivals (nut.js, FlaUI, WinAppDriver, Playwright, Windows-MCP, AHK). Return "clean" WITH evidence, OR a ranked list each with a proof.

RESOLUTION — met IFF this turn ALL Panel-A critics returned ZERO criticisms AND Panel-B ZERO features. If either raises ANYTHING, NOT met.

ON ANY FINDING: spawn Fable 5 ultracode agents IN PARALLEL, one per flagged seat — RESOLVE end-to-end: build, prove LIVE, ADD a test, COMMIT + PUSH. NEVER stop or ask permission; the /goal self-loops. Don't re-review fixes this turn — the next panels re-catch.

VERIFY LIVE (measure, don't assume): a wrong COM slot SEGFAULTS — prove every slot live, extend slot-gate.test.ts. bun test is broken for FFI — prove via example/*.integration.test.ts driving REAL apps that CLOSE every window (dispose != close). Perf = Bun.nanoseconds before/after + identical output + a regression gate; name isolated/in-proc/live-cross-proc. SEE rendered output for visual claims.

AGENTS.md IS LAW: surgical diffs, NO casts (fix the types), cast-free com.ts vcall, read .ptr inline, struct at the FFI call site, separate nullability audit, tsc 0 after every change, AI.md stays generic.

SHIP = COMMIT + PUSH every change (Conventional Commits, one per slice). Do NOT version-bump, publish, or touch server.json — the owner releases. Add modules to package.json files[]; keep findings/ current (dead-ends are CONSTRAINTS — re-confirm STILL a wall).

ANCHOR (STALE — RE-VERIFY): 63 policy-gated MCP tools (UMBRIEL_PROFILE readonly|safe|full). Do NOT author a successor prompt. FIRST ACTION: spawn BOTH panels NOW.
